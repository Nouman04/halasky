<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Hotel Booking Invoice - <%= pnr %></title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 30px;
      color: #333;
    }
    h1, h2 {
      color: #2c3e50;
    }
    .header, .section {
      margin-bottom: 30px;
    }
    .section-title {
      border-bottom: 2px solid #2c3e50;
      padding-bottom: 5px;
      margin-bottom: 10px;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 10px;
    }
    th, td {
      padding: 10px;
      border: 1px solid #ccc;
    }
    th {
      background-color: #f4f4f4;
    }
    .total {
      font-weight: bold;
      font-size: 18px;
      margin-top: 10px;
      text-align: right;
    }
  </style>
</head>
<body>

  <div class="header">
    <h1>Hotel Booking Invoice</h1>
    <p><strong>PNR:</strong> <%= pnr %></p>
    <p><strong>Check-in Date:</strong> <%= from %></p>
    <p><strong>Check-out Date:</strong> <%= to %></p>
  </div>


  <div class="section">
    <% if (hotelName) { %>
      <p>Hotel: <strong><%= hotelName %></strong></p>
    <% } %>

    <% if (hotelRoom) { %>
      <p>Room: <strong><%= hotelRoom %></strong></p>
    <% } %>

    <h2 class="section-title">Guests</h2>
    <table>
      <thead>
        <tr>
          <th>First Name</th>
          <th>Last Name</th>
          <th>Type</th>
          <th>Lead Guest</th>
          <th>Phone</th>
          <th>Email</th>
          <th>Age</th>
        </tr>
      </thead>
      <tbody>
        <% roomList.forEach(room => { %>
          <% room.guests.forEach(guest => { %>
            <tr>
              <td><%= guest.firstName %></td>
              <td><%= guest.lastName %></td>
              <td><%= guest.type %></td>
              <td><%= guest.leadGuest ? 'Yes' : 'No' %></td>
              <td><%= guest.phone || '-' %></td>
              <td><%= guest.email || '-' %></td>
              <td><%= guest.age || '-' %></td>
            </tr>
          <% }) %>
        <% }) %>
      </tbody>
    </table>
  </div>

  <div class="section">
    <h2 class="section-title">Payment Information</h2>
    <p><strong>Guarantee Type:</strong> <%= paymentDetail.guaranteeType %></p>
    <p><strong>Payment Type:</strong> <%= paymentDetail.type %></p>
    <p><strong>Card Code:</strong> <%= paymentDetail.cardCode %></p>
    <p><strong>Cardholder Name:</strong> <%= paymentDetail.firstName %> <%= paymentDetail.lastName %></p>
    <p><strong>Card Number (Last 4):</strong> <%= paymentDetail.cardNumber.slice(-4) %></p>
    <p><strong>Expiry Date:</strong> <%= paymentDetail.expiryMonth %>/<%= paymentDetail.expiryYear %></p>
  </div>

  <div class="total">
    Total Amount: <%= totalAmount %>
  </div>

</body>
</html>